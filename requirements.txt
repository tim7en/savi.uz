# Core package (savi_uz.data_sources, tradfi_universe, mapping_check, seed_groups)
# is standard library only. These are needed for the risk-map analysis.
numpy>=1.26
pandas>=2.2
yfinance>=0.2.40

# Shiller publishes ie_data.xls in the legacy .xls format, which openpyxl
# cannot read; xlrd 2.x reads .xls only, which is exactly what is needed.
xlrd>=2.0

# FactSet Earnings Insight is published only as a PDF; pdfplumber keeps the
# page-1 layout intact, which is what the Key Metrics patterns rely on.
pdfplumber>=0.11
